@{
    ViewBag.Title = "Página Inicial";
    Layout = "~/Views/Shared/_Layout.cshtml";

    var idioma = Session["Idioma"]?.ToString()?.ToLower() ?? "pt";
    Func<string, string> T = texto => Tradutor.Helpers.TradutorHelper.Traduzir(texto, idioma);
}

<div class="container mt-5">
    <div class="text-center mb-4">
        <h1 class="display-5 fw-bold">@T("Tradutor")</h1>
        <p class="lead text-muted">@T("Este sistema permite traduzir palavras e frases entre vários idiomas.")</p>
    </div>

    <hr class="mb-5" />

    <div class="mb-4">
        <h3 class="text-primary">@T("Como funciona?")</h3>
        <ul class="list-group list-group-flush">
            <li class="list-group-item">@T("Você pode cadastrar traduções em diferentes idiomas.")</li>
            <li class="list-group-item">@T("Depois, ao acessar o sistema, os textos serão exibidos automaticamente no idioma escolhido.")</li>
            <li class="list-group-item">@T("Também é possível traduzir frases completas usando o tradutor.")</li>
        </ul>
    </div>

    <div class="alert alert-warning shadow-sm">
        <h4 class="alert-heading">@T("Atenção!")</h4>
        <p>
            @T("Para que uma tradução seja encontrada no sistema, o texto original digitado deve ser exatamente igual ao que foi cadastrado no banco de dados.")
        </p>
        <p>
            @T("Isso inclui letras maiúsculas/minúsculas, espaços e pontuação. Por exemplo, 'Olá' é diferente de 'ola' ou ' Olá '.")
        </p>
        <hr>
        <p class="mb-0">
            <strong>@T("Importante:")</strong>
            @T("Se uma palavra ou frase ainda não tiver uma tradução cadastrada no banco de dados para o idioma escolhido, ela será exibida no texto original, sem tradução.")
        </p>
    </div>
</div>
