@using Tradutor.Helpers
@{
    ViewBag.Title = "Página Inicial";
    var idioma = Session["Idioma"] != null ? Session["Idioma"].ToString() : "en";
}

<h2>@TradutorHelper.Traduzir("Bem-vindo ao sistema!", idioma)</h2>
<p>@TradutorHelper.Traduzir("Esta página demonstra a tradução automática com base no idioma escolhido.", idioma)</p>
